[pytest]
testpaths = tests competition_core/tests
markers =
    model: requires local model artifacts and may run slowly
    slow: marks tests as slow (deselect with '-m "not slow"')
addopts = -ra -m "not model"
