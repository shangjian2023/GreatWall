schema_version: "1.0"

# This file is deliberately separate from training YAML. The primary detector
# accepts only model-loading and deterministic runtime settings from it.
model:
  target_base: "facebook/opt-125m"
  reference_base: "facebook/opt-125m"
  device: "auto"
  dtype: "float32"

runtime:
  seed: 42
