# Agent Instructions

Read and follow `CLAUDE.md` before changing this repository. It is the single project-level rule manual for Codex and Claude Code; do not duplicate those rules here.

Competition algorithm work defaults to `competition_core/`. Read `competition_core/README.md` and its local `AGENTS.md` before editing that path. Treat the older `src/` and `scripts/` detector as historical unless the user explicitly scopes work there.
